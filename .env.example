# Vercel AI Gateway — one key for every model. Never expose client-side.
# On Vercel, VERCEL_OIDC_TOKEN is injected automatically (no key needed).
# For local `vercel dev`, set a gateway key here (copy to .env.local):
AI_GATEWAY_API_KEY=
